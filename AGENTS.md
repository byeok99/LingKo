# 프로젝트 작업 지침

## 프로젝트

Spring 백엔드 + Flutter 앱 프로젝트.

## 운영 방식

이 프로젝트는 하나의 Codex 세션과 하나의 작업 흐름을 기준으로 운영한다.

- 별도의 터미널 분할이나 고정된 역할 분담을 작업 전제로 두지 않는다.
- 요구사항 정리, 관련 파일 확인, 구현, 명령 실행, 자체 리뷰, 검증, 변경 요약을 현재 Codex 세션에서 연속해서 처리한다.
- 서버 실행, 로그 확인, 테스트처럼 터미널 작업이 필요한 경우에도 가능한 범위에서 현재 세션의 명령 실행 기능을 사용한다.
- 장기 실행 프로세스나 사용자 환경 확인이 필요하면 별도 터미널을 가정하지 않고, 필요한 명령과 확인 지점을 사용자에게 안내한다.
- 별도 AI 세션에 작업을 위임하지 않는다.
- Codex 공식 subagent는 사용자가 명시적으로 요청한 경우에만 사용한다.
- 토큰 절약을 위해 불필요한 반복 탐색, 전체 파일 열람, 과도한 설명을 피한다.

## RTK 사용

Rust Token Killer(`rtk`)는 설치되어 있으나 자동 hook은 적용되어 있지 않다.

- shell 명령은 가능한 한 `rtk` prefix로 실행한다. 예: `rtk git status`, `rtk ./gradlew test`
- raw 출력이 필요하거나 `rtk` 필터링이 방해되면 `rtk proxy <cmd>`를 사용한다.
- `rtk gain` 통계는 tracking DB 접근 상태에 따라 실패할 수 있으므로 필수 검증으로 보지 않는다.
- 자동 hook 적용은 사용자 명시 요청이 있을 때만 수행한다.

## 작업 범위

- 수정 전에는 확인할 파일 범위를 짧게 말한다.
- 전체 코드베이스를 무작정 탐색하지 말고, 요청과 관련된 파일 중심으로 확인한다.
- 대규모 리팩토링보다 요청 범위 내 최소 변경을 우선한다.
- 관련 없는 사용자 변경은 되돌리지 않는다.
- 터미널 실행 스크립트, 빌드 설정, 앱 설정, 소스코드는 사용자가 명시적으로 요청한 범위에서만 수정한다.

## 폴더별 작업 이력

수정이 발생한 모든 프로젝트 폴더에는 이후 작업자가 이전 변경 맥락을 빠르게 확인할 수 있도록 작업 이력을 남긴다. 이 규칙은 `app/`, `backend/`, `docs/`, `.codex/`, `.agents/`, 루트 설정 파일을 포함한 저장소 전체에 적용한다.

- 이력 파일 이름은 각 폴더의 `WORK_LOG.md`로 통일한다.
- 최소 관리 단위는 Git이 관리하는 파일 또는 현재 프로젝트 작업 파일이 직접 위치한 디렉터리다.
- 최소 관리 단위에 해당하는 모든 디렉터리에는 실제 변경 이력이 아직 없어도 `WORK_LOG.md`를 미리 둔다.
- 새 파일이나 새 디렉터리를 추가할 때 해당 직접 디렉터리에 `WORK_LOG.md`가 없으면 같은 작업에서 함께 생성한다.
- 작업 시작 시 수정 대상 후보 폴더가 좁혀지면 해당 폴더의 `WORK_LOG.md`를 먼저 확인해 이전 변경 맥락, 남은 리스크, 검증 이력을 파악한다.
- 상위 폴더의 정책이나 맥락이 영향을 줄 수 있으면 가장 가까운 상위 `WORK_LOG.md`도 필요 범위 안에서 확인한다.
- 관련성이 낮은 폴더의 `WORK_LOG.md`를 전체 탐색하지 않는다.
- 파일을 수정한 폴더마다 해당 폴더의 `WORK_LOG.md`를 업데이트한다.
- 상위 폴더 하나에 몰아서 기록하지 말고, 변경된 파일이 위치한 각 직접 폴더에 별도의 `WORK_LOG.md`를 둔다.
- 예를 들어 `app/lib/screens/foo.dart`, `backend/src/main/java/.../Bar.java`, `docs/api.md`, `.codex/agents/main.md`를 함께 바꿨다면 `app/lib/screens/WORK_LOG.md`, 해당 백엔드 Java 파일의 직접 폴더 `WORK_LOG.md`, `docs/WORK_LOG.md`, `.codex/agents/WORK_LOG.md`를 각각 업데이트한다.
- 여러 파일을 함께 바꾸더라도, 변경된 파일이 속한 직접 폴더마다 이력을 남긴다.
- 같은 기능 변경이 여러 폴더에 걸치면 각 폴더의 이력에는 해당 폴더에서 실제로 한 일만 짧게 기록한다.
- 상위 `WORK_LOG.md`는 하위 폴더의 상세 변경을 대신하지 않는다. 상위 폴더 자체의 파일을 수정했거나, 여러 하위 변경을 조율한 상위 맥락을 남길 필요가 있을 때만 추가로 기록한다.
- 루트 파일(`AGENTS.md`, `.gitignore`, 루트 설정 파일 등)을 수정한 경우에는 루트 `WORK_LOG.md`를 업데이트한다.
- 새 폴더를 만들거나 기존 폴더에 `WORK_LOG.md`가 없으면 새로 만든다.
- 이력 항목은 최신 항목이 위로 오게 추가한다.
- 각 항목에는 날짜, 작업 요약, 변경 파일, 검증 결과, 남은 리스크를 짧게 적는다.
- 코드 변경 없이 단순 조회, 일회성 로그 확인, 포맷 결과 확인만 한 경우에는 이력을 남기지 않는다.
- 루트 지침, 전역 설정, 문서 정책처럼 특정 하위 폴더에 속하지 않는 변경은 루트 `WORK_LOG.md`에 기록한다.
- 비밀값, 토큰, 개인식별정보, 긴 로그 전문은 이력에 남기지 않는다.
- `build/`, `bin/`, `.dart_tool/`, `.gradle/`, `Pods/`, `.cxx/`, `logs/`, `tmp/` 등 생성물·캐시·의존성·런타임 출력 폴더는 관리 대상에서 제외한다.
- Android `res/`, Xcode의 `.xcassets`, `.xcodeproj`, `.xcworkspace`처럼 임의 파일 추가가 빌드나 도구 동작에 영향을 줄 수 있는 전용 번들 내부에는 `WORK_LOG.md`를 만들지 않는다. 해당 내부 파일 변경은 가장 가까운 안전한 상위 폴더의 `WORK_LOG.md`에 기록한다.

권장 형식:

```md
## YYYY-MM-DD - 작업 요약

- 변경 파일: `path/to/file`
- 내용: 무엇을 왜 바꿨는지 1-3줄
- 검증: 실행한 명령 또는 미실행 사유
- 리스크: 남은 확인 사항, 없으면 `없음`
```

## 개발 원칙

- 수정 전 실제 디렉터리 구조와 기존 스타일을 확인한다.
- 백엔드와 앱이 함께 쓰는 기능은 API 계약을 먼저 맞춘다.
- API Key, 비밀번호, 토큰은 코드에 하드코딩하지 않는다.
- Entity를 API 응답으로 직접 노출하지 않는다.
- Flutter는 화면, 상태관리, API client를 가능한 한 분리한다.
- 입력값은 시스템 경계에서 검증한다.
- 오류는 사용자에게 필요한 메시지와 개발자가 확인할 수 있는 원인을 구분해 다룬다.
- 테스트 또는 실행 확인 없이 완료했다고 단정하지 않는다.
- 테스트를 못 하면 이유와 수동 검증 방법을 보고한다.

## 코드와 문서 동기화

코드 변경으로 기능, API 계약, 설정, 데이터 모델, 운영 절차 또는 구현 상태가 달라지면 같은 작업에서 관련 활성 문서를 함께 갱신한다.

- 구현 전에는 변경 대상 기능을 설명하는 README, API 문서, architecture 문서, 요구사항, roadmap, technical debt, 실행·배포 가이드를 관련 키워드로 좁혀 확인한다.
- 구현 후에는 기존 문서에 `미구현`, `부분 구현`, `현재 제한`, `후속 작업`처럼 새 코드와 충돌할 수 있는 상태 문구가 남아 있는지 다시 검색한다.
- endpoint, 요청·응답 DTO, validation, error code, 인증 흐름이 바뀌면 Backend와 App의 API 계약 문서 및 예시를 같은 작업에서 수정한다.
- 환경변수, 기본값, migration, 외부 서비스 설정, 실행 명령이 바뀌면 `.env.example`, application 예시, 로컬 개발·배포 문서를 함께 수정한다.
- 기능이 완료되면 roadmap·backlog·technical debt·요구사항의 상태를 완료로 갱신하고, 남은 운영 검증이나 부하 테스트는 구현 미완료와 구분되는 후속 리스크로 기록한다.
- 더 이상 유효하지 않은 설명은 삭제하거나 현재 동작으로 교체한다. 과거 기록이 필요하면 활성 문서에 남기지 말고 기존 archive 정책을 따른다.
- 관련 문서를 수정한 각 직접 폴더의 `WORK_LOG.md`에 변경 이유, 검증 결과, 남은 리스크를 기록한다.
- 문서 변경이 필요 없다고 판단한 경우에도 영향도 확인 결과와 근거를 완료 보고에 짧게 남긴다.
- 코드와 활성 문서가 일치하지 않으면 테스트가 통과했더라도 작업 또는 Issue를 완료로 처리하지 않는다.

## 트러블슈팅 문서화 규칙

성능, 안정성, 데이터 정합성, 운영, 보안 또는 주요 사용자 흐름에 영향을 주는 문제를 수정할 때는 영향도와 재사용 가치를 판단해 코드 변경과 같은 PR에서 `docs/troubleshooting/`의 트러블슈팅 노트를 작성하거나 갱신한다. 모든 오류를 문서화하지 않으며 재발 가능성, 사용자·운영 영향, 조사 난이도와 학습 가치를 기준으로 선별한다.

### 문서화 판단

다음 중 하나 이상을 만족하면 문서화 대상 후보로 본다.

- API 응답시간, 처리량, CPU, 메모리, DB query 수에 의미 있는 영향을 주는 병목
- N+1, 반복 query, 잘못된 index, 느린 pagination 등 명확한 DB 성능 문제
- memory leak, thread·connection pool 고갈, 파일·socket 미해제 등 자원 문제
- 동시성, 중복 처리, transaction, lock, cache 불일치로 발생하는 데이터 정합성 문제
- 사용자 데이터 유실·중복 저장·잘못된 사용자 귀속 등 심각한 기능 문제
- 로그인, token 만료, 권한, 녹음, 업로드, 평가처럼 주요 사용자 흐름을 막는 문제
- 반복 발생하거나 고객 문의·CS로 이어질 가능성이 높은 문제
- 외부 서비스 장애, timeout, retry, Rate Limit 등 운영 장애
- 배포, migration, 환경변수, Secret, rollback 등 운영 환경 문제
- 보안·개인정보에 영향을 주는 문제
- 원인 규명이 복잡하거나 해결 과정과 의사결정을 다른 작업에 재사용할 수 있는 문제

문서화 여부가 애매하면 다음 질문을 사용한다.

1. 이 문제를 3개월 후 다시 만났을 때 문서가 도움이 되는가?
2. 다른 개발자가 같은 원인을 다시 조사할 가능성이 있는가?
3. 사용자, 데이터, 비용, 성능 또는 운영에 영향을 주는가?
4. 단순 수정만으로는 원인과 예방책이 코드에 충분히 드러나지 않는가?

두 개 이상이 `예`이면 트러블슈팅 노트를 작성한다. 하나 이하라도 SEV-1·SEV-2, 보안·개인정보 또는 데이터 유실 위험은 반드시 문서화한다.

### 문서화 제외

다음 문제는 반복성이나 사용자·운영 영향이 크지 않으면 별도 노트를 만들지 않는다.

- 단순 오탈자, 변수명 수정, IDE 경고 제거
- 원인과 수정이 즉시 명확한 import·compile 오류
- 한 줄 수준의 명백한 null 검사 누락
- 운영 코드에 영향이 없는 테스트 기대값·mock 설정 실수
- 재현되지 않았고 원인도 확인되지 않은 추측성 문제
- 기존 문서와 원인 및 해결책이 동일한 문제

기존 문서와 같은 원인이면 새 파일을 만들지 않고 기존 문서에 새로운 발생 조건, 사례, 증거와 검증 결과를 추가한다.

### 작성 위치와 형식

- 문서 경로: `docs/troubleshooting/`
- 인덱스: `docs/troubleshooting/README.md`
- 파일명: `YYYY-MM-DD-<problem-kebab-case>.md`
- 심각도: `SEV-1` 서비스 사용 불가·데이터 유실/노출·광범위 영향, `SEV-2` 핵심 기능 실패·상당수 영향·즉시 대응, `SEV-3` 일부 사용자/특정 조건의 기능·성능 저하, `SEV-4` 영향은 작지만 재발 방지 가치가 있는 문제
- 필수 내용과 상세 템플릿은 `docs/troubleshooting/README.md`를 따른다.
- 관련 GitHub Issue와 PR을 반드시 연결하며 연결되지 않은 노트가 있으면 작업을 완료 처리하지 않는다.
- 측정하지 않은 수치, 확인하지 않은 원인과 재현되지 않은 추측을 사실처럼 기록하지 않는다.
- 성능 개선은 가능하면 변경 전후 query 수, 응답시간, 메모리, 실행 계획을 같은 조건에서 측정한다. 측정하지 못한 항목은 `미측정`과 이유를 기록한다.

### 작업 절차

1. 문제 수정 전에 인덱스와 관련 키워드를 검색해 기존 노트의 중복 여부를 확인한다.
2. 재현 조건, 로그, query, metric, stack trace 등 비밀값을 제외한 증거를 먼저 확보한다.
3. 임시 조치와 근본 해결을 구분하고 선택하지 않은 대안과 판단 근거를 기록한다.
4. 문제를 수정하는 PR에서 노트, 인덱스, 관련 코드·운영 문서를 함께 작성하거나 갱신한다.
5. 검증 명령과 실제 결과, rollback 절차, 재발 방지 test·monitoring을 기록한다.
6. 관련 Issue·PR 연결, 문서 사실 검증과 해당 폴더 `WORK_LOG.md` 갱신이 끝나야 완료로 처리한다.

## 코드 주석 원칙

Backend와 App에서 새로 작성하거나 의미 있게 수정하는 코드는 다음 작업자가 구현 결정을 복원할 수 있도록 의도 중심 주석을 함께 작성한다.

- 설명 문장은 한국어로 작성한다. API, DTO, Flutter, token처럼 코드와 직접 대응하는 표준 기술 용어와 식별자는 필요한 경우 원문 표기를 유지한다.
- Java 공개 class/interface/record와 Dart 공개 class·주요 service/API에는 Javadoc 또는 Dartdoc으로 해당 코드의 **의도와 책임**을 설명한다.
- API endpoint, domain service, repository query, 상태 전이, 외부 연동 경계에는 입력·출력의 의미와 해당 계층이 책임을 가지는 이유를 설명한다.
- 보안, 트랜잭션, 동시성, 캐시, retry, fallback, timeout, 데이터 정규화처럼 구현 선택지가 여러 개인 로직에는 **왜 현재 방식을 선택했는지**와 보호하려는 invariant를 짧게 남긴다.
- 상수, enum, 상태값, nullable 값이 업무적으로 특별한 의미를 가지면 그 의미와 허용 범위를 설명한다.
- 테스트 파일 또는 테스트 class에는 어떤 계약, 회귀, 실패 조건을 보장하는지 설명한다.
- 주석은 코드가 하는 일을 문법적으로 반복하지 않는다. 이름만 읽어도 자명한 DTO field, getter/setter, 단순 위임, 일반적인 framework annotation에는 불필요한 주석을 붙이지 않는다.
- 구현이 변경되면 같은 작업에서 관련 주석도 갱신한다. 코드와 불일치하거나 과거 동작을 설명하는 주석은 남겨두지 않는다.
- 비밀값, 실제 token, 개인식별정보, 환경별 credential은 예시 주석에도 기록하지 않는다.
- 이미 적용된 Flyway migration처럼 checksum이나 도구 동작에 영향을 주는 불변 파일은 주석만을 위해 수정하지 않는다. 목적 설명은 대응 entity, 새 migration 또는 architecture 문서에 남긴다.

## 구현 체크리스트

- Spring 변경은 DTO, validation, exception handling, transaction, migration 영향을 확인한다.
- Flutter 변경은 화면, 상태, API client, model 경계를 확인한다.
- 외부 연동은 Azure, Replicate, AWS S3, FFmpeg, MySQL 관련 설정과 비밀값 노출 위험을 확인한다.
- 변경 범위가 백엔드와 앱을 동시에 건드리면 요청/응답 형식, 에러 포맷, 인증 흐름을 먼저 정리한다.
- 새 기능이나 버그 수정은 가능한 한 가장 작은 의미 있는 테스트를 함께 추가하거나 안내한다.
- 새 코드와 변경 코드의 주석이 의도, 의미, 이유, 선택을 설명하며 실제 구현과 일치하는지 확인한다.
- 관련 활성 문서의 기능 상태, API 계약, 설정 예시와 현재 구현이 일치하는지 확인한다.
- 수정한 문제가 트러블슈팅 문서화 기준에 해당하는지 판단하고, 해당하면 노트·인덱스·Issue·PR 연결을 확인한다.

## 리뷰와 검증

단일 AI가 수정 후 자체 점검한다.

- correctness, regression, security, missing tests를 우선 확인한다.
- 스타일보다 버그, API 계약 불일치, 보안 문제, 테스트 공백을 먼저 본다.
- 가능한 검증과 로그 확인은 현재 Codex 세션에서 직접 수행한다.
- 사용자 환경에서만 재현할 수 있는 검증은 실행 명령과 확인 지점을 안내하고, 사용자가 제공한 핵심 결과를 바탕으로 후속 점검한다.
- 로그가 필요하면 전체 로그를 요구하지 말고 핵심 에러 구간, 실패 명령, 재현 조건만 요청한다.
- 검증 명령은 변경 범위에 맞게 최소로 제안한다.

권장 검증 예:

- Backend compile: `cd backend && ./gradlew compileJava`
- Backend unit tests: `cd backend && ./gradlew test`
- Backend integration tests: `cd backend && ./gradlew integrationTest`
- Flutter analysis: `cd app && flutter analyze`
- Flutter tests: `cd app && flutter test`

## 브랜치와 커밋 원칙

브랜치와 PR은 phase 단위가 아니라 기능 단위로 나눈다.

- 하나의 브랜치는 독립적으로 리뷰, 테스트, 배포 판단이 가능한 기능이나 운영 변경 하나만 포함한다.
- backend API, Flutter 화면/API 연동, native 설정, 문서/운영 스크립트는 서로 다른 검토 단위이면 별도 브랜치로 분리한다.
- 여러 기능이 순서 의존성을 가지면 stacked PR로 만들고, 각 PR의 base branch를 명확히 둔다.
- phase 번호는 작업 추적용일 뿐 브랜치명, PR 범위, 커밋 범위의 기준으로 쓰지 않는다.
- 커밋은 작은 논리 단위로 만든다. 한 커밋에는 하나의 목적과 그 목적을 검증하는 테스트만 포함한다.
- 커밋 전 staged diff가 여러 기능을 섞고 있으면 커밋하지 말고 기능별로 분리한다.
- 대규모 변경을 push하거나 PR 생성하기 전에 변경 파일 목록을 확인하고 기능 단위 분리가 필요한지 사용자에게 보고한다.
- PR 본문에는 포함 기능, 제외한 후속 기능, 검증 결과, stacked PR 의존 관계를 명시한다.

## 참고 파일

필요할 때만 아래 파일을 참고한다.

- 현재 Codex 역할: `.codex/agents/main.md`
- 공통 규칙 후보: `.codex/rules/*.md` 또는 `~/.codex/rules/*.md`
- 작업 절차 후보: `.codex/skills/*.md` 또는 `~/.codex/skills/*.md`

rules/skills 디렉터리는 필요한 파일명을 찾기 위한 목록 조회만 먼저 수행한다. 파일 내용은 현재 작업과 직접 관련이 있을 때만 읽는다.

## 완료 보고

작업 완료 시 아래만 짧게 보고한다.

1. 작업 요약
2. 변경 파일
3. 검증 결과
4. 남은 리스크
5. 다음 액션
