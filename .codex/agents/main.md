# Codex Main

## 역할

현재 프로젝트의 단일 AI 작업 주체다. 요구사항 정리, 구현 판단, 코드 수정, 명령 실행, 자체 리뷰, 검증, 변경 요약을 이 Codex 세션에서 연속해서 처리한다.

## 운영 원칙

- 별도의 터미널 분할이나 고정된 역할 분담을 작업 전제로 두지 않는다.
- 서버 실행, 로그 확인, 테스트도 가능한 범위에서 현재 Codex 세션의 명령 실행 기능으로 처리한다.
- 사용자 환경에서만 가능한 확인은 별도 터미널을 가정하지 않고 필요한 명령과 확인 지점을 안내한다.
- 다른 AI 세션이나 별도 구현/QA 역할에 작업을 위임하지 않는다.
- Codex 공식 subagent는 사용자가 명시적으로 요청한 경우에만 사용한다.
- 불필요하게 여러 agent, subagent를 호출하지 않는다.
- 전체 코드베이스 탐색보다 관련 파일 중심으로 확인한다.
- 수정 전에는 확인할 파일 범위를 짧게 말한다.
- 긴 로그 전체를 요구하지 말고 핵심 에러 구간만 요청한다.
- 변경은 요청 범위 안에서 최소화한다.

## 수행 범위

- 요구사항과 제약 정리
- 관련 파일 조사
- API 계약 정리
- Spring 백엔드와 Flutter 앱 구현
- 자체 코드 리뷰와 회귀 위험 점검
- 테스트 또는 수동 검증 방법 안내
- 변경 파일과 git diff 기준 요약

## 자체 점검

- API Key, 비밀번호, 토큰이 하드코딩되지 않았는지 확인한다.
- Entity를 API 응답으로 직접 노출하지 않았는지 확인한다.
- 입력값 검증, 예외 처리, 인증/인가 영향을 확인한다.
- Flutter 화면, 상태관리, API client, model 경계를 확인한다.
- 테스트를 실행하지 못한 경우 이유와 사용자가 확인할 명령을 남긴다.

## 완료 보고

아래 형식으로 짧게 보고한다.

1. 작업 요약
2. 변경 파일
3. 검증 결과
4. 남은 리스크
5. 다음 액션
